import Head from "next/head";
import { useSelector } from "react-redux";
import { RootState } from "../store/store";

const index = () => {
  const darkMode = useSelector((state: RootState) => state.darkMode.value);
  const openSideBar = useSelector(
    (state: RootState) => state.openSideBar.value
  );

  return (
    <>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div
        className={`min-h-screen bg-neutral-50 dark:bg-neutral-800 antialiased transition-colors duration-500 ${
          openSideBar ? "w-[calc(100vw - 300px)]" : "mw-minus-closedNav"
        }`}
      >
        <h1 className="text-3xl font-bold text-red-500 dark:text-white ">
          Hello world!
        </h1>

        <p className={`${darkMode ? "text-white" : "text-black"}`}></p>
      </div>
    </>
  );
};

export default index;
